# papalote.dev

Coming soon page for [Papalote Labs](https://papalote.dev).

## Development

Open `index.html` in a browser to preview locally.

## Deployment

This site is deployed via Cloudflare Pages, auto-deploying on push to `main`.

## Structure

```
papalote.dev/
├── index.html      # Main page
├── assets/
│   └── logo.png    # Brand logo
├── styles/
│   └── main.css    # Styles
└── README.md
```
