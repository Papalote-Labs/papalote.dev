# Cloudflare Pages build configuration (source of truth for the git-connected
# papalote-dev project). Only the build output directory is published, so repo
# files like .git, .github, and this config are never served on papalote.dev.
name = "papalote-dev"
pages_build_output_dir = "./public"
compatibility_date = "2024-01-01"
